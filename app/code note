网络爬虫


%替换成//
开发到获取数据时，返回的数据一直不对，无论把nm怎么编辑、encode都无用，返回的值都是全的（相当于nm值为空，别人我怎么知道的。。。），相同的链接在电脑浏览器和手机浏览器上都可以正常打开。
后来想是否是手机网络问题，4g网和wifi情况下都调试过，并不是这个问题。
之后换到另一个网址，测试n值，发现有不同的情况了！！！404错误！！！可以网址放在浏览器上是正常的，一筹莫展。
突然，看到了大光头[琦玉]，心头一亮！是不是获取数据的方法错了，然后想到了get和post方法，因为项目里用的MyHttpConnectionTask类是用的公司项目的，于是去百度了。
1:> URL请求的类别:
分为二类,GET与POST请求。二者的区别在于：
     a:) get请求可以获取静态页面，也可以把参数放在URL字串后面，传递给servlet，
     b:) post与get的不同之处在于post的参数不是放在URL字串里面，而是放在http请求的正文内。
引用于http://www.blogjava.net/supercrsky/articles/247449.html，谢谢。
获取的数据为静态页面，用get就可以了，切换之。成功！
